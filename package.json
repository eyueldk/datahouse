{
  "name": "datahouse",
  "module": "index.ts",
  "devDependencies": {
    "@types/bun": "latest",
    "prettier": "^3.8.1",
    "turbo": "^2.5.4"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "packageManager": "bun@1.3.11",
  "private": true,
  "scripts": {
    "build": "turbo run build --filter=@datahouse/studio --filter=datahouse",
    "dev": "turbo run dev --filter=@datahouse/studio --filter=datahouse",
    "format": "prettier --write ."
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ]
}
